# lab/jalon411 — the RM cartography & the power-enforcement hunt (the master index)

The wave of passes 4.14 → 4.34: from the rm.elf dispatch cartography to
the EDPp enforcement hunt. Every pass = one findings file + the
instruments (the `v4xx_*.py` scripts + their JSONs). **The entry point
for the next session = pass 4.26 (the recv capture, armed).**

## The passes

| pass | findings | what it established | the verdict |
|---|---|---|---|
| 4.14 | datflow | the vtable installs, the companion census | the bases = carried, not formed |
| 4.15 | aliased-slot-and-o2 | the 0x588 alias, the O2 verdict | no dominant allocator |
| 4.16 | boundary-proof | the recursive descent, the 66 % verified | the dispatch-driven architecture |
| 4.17 | stategraph | the 665 slots, the derivation graph | one state shape, many bases |
| 4.18 | hubfill | the hub fills, the first X resolutions | 0 named targets |
| 4.19 | prov588-entrypoints | the full provenance table | the family = 3 semantics |
| 4.20 | rpc-anchors | **the RPC dispatch table found (1156 entries)** | the anchors survive the runtime-bind |
| 4.21 | pfm-worker-runtime-data | the mW scan, the ceiling = runtime data | 280000 = zero hits in rm.elf |
| 4.22 | power-transport | the open kernel sends no tables | the BIT parser = the closed x86 blob |
| 4.23 | transport-edpp | the transport instrumented end to end | the machine degraded — restored |
| 4.24 | payload-fieldmap **(FALSIFIED 4.23's assumption)** | the 1616 = cmd 0x2080d031, the handler = unconsumed | 250000 @104 = rides unread |
| 4.24 | edpp-flow | the EDPp lifecycle modeled from the open source | the client vs the platform limits |
| 4.24 | gsp-lz | the LZ4 codec + the container built and verified | 13/13, byte-exact |
| 4.24 | citation-audit | every 4.24 claim re-executed | clean |
| 4.25 | x86-marshal | win.elf falsified (a RISC-V artifact), the 0x2080d0 family = 19 controls | the offsets = HYPOTHESIS |
| 4.25 | recv-edpp | the GSP→CPU completion chain proven | **the 96-B prediction armed — the capture = 4.26** |
| 4.27 | gspbin-container | gsp_ga10x.bin acquired, the provenance proven | byte-exact rebuilder delivered |
| 4.28 | x86-substrate + the hunt closed | **nv-kernel.o_binary acquired; the issuer = NVML userspace PROVEN** | **the "250000" = the LACT clock-VF offset, NOT power** |
| 4.29 | lz-real-roundtrip | **the pair premise falsified at phdr level (comp-725KB = vgpu.elf, flat); no NVIDIA LZ4 stream in 1.81 GB scanned** | codec unchanged, T6 = the real-bytes round-trip, permanent; recv76_analyze delivered |
| 4.30 | gspbin-pipeline | **the GFW directory grammar decoded (13 records, the +0x6d000 bias law, 5/5 byte-exact containments); the u32 patch premise falsified — the six 250000 = lui+addi pairs; the patch landed (18 B differ / 48 rewritten); the load path source-proven (GA104 = gsp_ga10x.bin)** | gspbuild patchrm + R14-R17; the signature coverage = UNDECIDABLE-BY-BYTES |
| 4.31 | booter-verify-hunt | **the LS-signature verify is NOT in our booter libos ELF (proven: no crypto CSRs 0x7d5-9, no SBI crypto ecall, no SHA/RSA constants, no 0xc0deca7e; the paper's 0x29C4/0x4d4/0x2e80/0x7dd9 are all < 0x8000 = BROM addresses); our secure interface = SBI ecalls (a7=0x900001EB, a6∈{0,7,8,9,0xa}, fail=a7=8) + the nvriscv region CSRs 0x5ca-0x5d1/0x8d0; TWO booters separated: the .ko BooterLoad is ENCRYPTED (IMAGE_PROD 0x87d7, NUM_SIGS=2, BROM-RSA-covered — patch refused by construction) while our plaintext libos booter = the gsp_ga10x.bin first section @container 0x40 (the container = a RISC-V ELF, new proof); booter_emu.py runs the real image (5/5) and the directed patch demo works: the 0x1014DC bounds check original→FAIL-oracle vs NOP'd→clean ret, exactly 4 bytes differ** | v431_booter_hunt 17/17; patch_booter locate-ko/locate-gsp/patch-gsp 5/5; tools/booter_emu.py 5/5 |
| 4.34 | time-queue-and-430-closure | **PART A the 4.30 closure: the split-form finder enters gspbuild (capstone clobber check) and finds EXACTLY the 7th site (lui a4 @rm+0xb99c4a, addi @+0xb99c52, gap 8); the 7/7 patch produced (21 B differ: 18 contiguous + 3 split, the `sub s2,s10,s2` byte-preserved, all 7 pairs re-decode 280000; artifact fwimage-77 sha 5962342b…, outside the repo) — VERDICT REVERT for any boot: 4.32 proved TIME-not-power, the runbook now REFUSES the 6/6/7 half-turned container and gates the 7/7 behind RUNBOOK_77_ACK=1; PART B the 34 c.lui-100000 bodies classified by first consumer: 6 CALL-ARG (c.jalr), 4 MUL (×1e5/div converters), 5 STORE-DATA (state fields, +0x5d0), 3 COMPARE (one vs a rdtime delta!), 8 ARITH, 11 slice-interrupted (4 proven c.j value-blocks, 0 dead) — 100000 = the RM's fine time quantum, never a power knob; PART C the TICK RATE PROVEN: 451 rdtime reads; the literal chain @0x1bc75f0-76 `s2=1e9 → divu → seconds → mul 0xF4240(1e6) → µs` ⇒ 1 tick = 1 ns (1 GHz); 31.25 MHz = ZERO hits in code AND data — the 4.32 µs ladder is the policy layer on the nanosecond counter; the 100000-vs-rdtime threshold = 100 µs** | v434a_patch77 + v434b_cl100_bodies + v434c_tickrate (+ JSONs); gspbuild extended (R18-R20, S1-S8); tests 27 PASS / 0 FAIL; 6 instrument lessons (the zlib-packed map, the capstone register-NUMBER identity, the c.lui rd-field, the linear-slice honesty, the load-dest ≠ read, the split patch = two separate writes) |
| 4.35 | optimization-hunt2 | **the optimization hunt, round 2 — the knob cards + the SAFE lane proven: (A) the 60 knobs carded (unit hypothesis + risk class + owner fan-in; the banked counts reproduce): 1000000 = TIME-CONVERSION DO-NOT-TOUCH-BLIND, 10000000 = TIME-QUANTA (10 ms vs the 1-ns tick), a coherent CLK-270k-FAMILY of 14 knobs (270000 × {1..100}), the GATED-TUNABLE shortlist ranked by leverage (500000/100000000/4000000 @fan-in-794 region 0x10844a4; 1435840000 = a 1.43584-GHz clock threshold with a 16-entry data twin @0x1c0d38c); (B) THE REGKEY LANE = the no-patch lever, PROVEN: 864 Rm*/RM* names in the firmware, 251 with direct PIC code xrefs (366 xrefs, 291 PASS-TO-CALL — the lookup-by-name pattern cited: string→a1→call→status gate), name-pointer tables in the wild (RmVgpcSkyline ×27+masks @0x1d858b8, RmCePceMap @0x1c492e0), FNV-1a-32 hash machinery (basis 0x811C9DC5 ×3, hash-context init window), the top tunables named (RML2MaxWaysSysmem = the L2-ways partitioning knob, RmClk2Enable, RMUseTc0NonCoherent…); (C) the data twins: the 22×1000000 default table @0x404b4b0 (the top knob's compiled defaults), the 2^n size-class ladders @0x1c4ad68/0x1c4ab68, the duplicated config blocks (the clone mass's data twin), NVDUMPCONFIGSIG at the data base; the d4d856ff ×666 DECODED = a live fill-value table (u32 0xff56d8d4 among descending 0xff57xx records, step ≈ −0x24) — the lane-D 'waste' reading corrected for this family (the banked 666 = the a_img-aligned 16-B unit, reproduced exactly); the two VA universes formalized (runtime = campaign + 0x38, from the container phdrs)** | v435a_knobcards + v435b_regkeys + v435c_datacensus (+JSONs); the 280 W rm.elf lane stays CLOSED (4.34 REVERT) — round 2 adds the SAFE host-regkey lane and refuses the time-math knobs by card |
| 4.36 | regkey-flow | **the regkey-to-knob flow map + the function owners + the slice resolution — and the CAMPAIGN VERDICT: the static hunt has reached its floor. TASK A (v436a): the regkey canal is NOT in-situ — 0/12 shortlist knobs carry a regkey xref in their ret-bounded function AND 0/12 at covered-region level (331 distinct xrefs windowed; the wider net nets zero); the field flow cartographed instead (400000 -> sd a5, -0xa0(s0) @0x5dcfc8 with 5 same-field stores, 1250000 -> sw a5, 0x274(a0)/0x27c(a0) two adjacent fields; zero ret-stores of a0 anywhere); the census discipline held (the banked 4.32 counts, the 36 c.lui site-equal to v434b, the regkeys 864/251/366, the RmValidateClientData xref re-decoded, the PIC edges 148249 with the banked callee 0x188EF44 fan-in 50 exact); the C.LUI byte-probe settles the layout: QUADRANT 1, funct3 011, FULL 5-bit rd at [11:7] (0x6a61 -> s4). TASK B (v436b): 18/300 sites get a ret-bounded window, ALL with PIC fan-in 0 (leaf fragments and value-blocks, not call-entered prologues); zero RPC-anchor ties, zero name-pointer ties at region level — the 4.35a region cards remain the finest static attribution. TASK C (v436c): the 11 (12 re-derived, delta 1 documented) interrupted sites re-attacked with a FORKING CFG slice — 5 BOTH-ARMS-AGREE:ARITH-CHAIN (the 4.34 value-blocks confirmed at CFG level + 0x1280da4 newly promoted), 7 UNRESOLVED with the mechanism cited (backward polling-loop edges — the rd is consumed in a later iteration; the HYPOTHESIS label stands). VERDICT: the SAFE lane = the host regkey experiments on STOCK firmware (the 4.35 tunables); the real data = the 4.26 capture; the 280 W rm.elf lane stays CLOSED** | v436a_regkey_flow + v436b_funcowners + v436c_slice7 (+JSONs) + findings-4.36-regkey-flow.md; 6 instrument lessons (the byte-probe before the bit-layout faith, the two-parity u32-view trap, the flat-equality format matching, fork-less slices under-report loops, a negative cross is a result, echo-proof instrument writing) |
| 4.37 | breakday-package | **the break-day package — the one reachability probe the campaign never ran (who REFERENCES the data twins?) + the founder's two boot days armed. PART A: the code-side probes ALL zero (absolute lui+addi pairs 0; lui+direct-access 0 with 0 page-bases in reach; PIC auipc refs 0; indexed access 0) — the widened diagnostic banks the boundary: 24 lui values land in the data LOAD (232 sites, 0x4000000 x180 = the segment base, 0x4040000 x2 = T1's page), auipc+addi into data = 0 over all 416,206 auipc (the data LOAD is never PIC-addressed). PART B: the data-side probe = THE RESULT — 25 u64 pointers from the data LOAD land in the 22x1000000 window (one per default record, three structural families by stride; the 25th caught by the 0x38-universe padding at 0x404b4a8) = the defaults are DESCRIPTOR-LINKED (instance descriptors in the data LOAD), the fill path = the RPC object-create path the 4.26 capture watches; the knob card unchanged (1000000 stays the DO-NOT-TOUCH-BLIND time family). PART C: the regkey EXPERIMENT CARDS — 17 cards (xrefs>=3 minus the REFUSED gate: RmClockUprocSecurityCheck / RmAllowChannelCreationOnPendingReset / RmDisableFbflcnDevinitBoot, reasons named), each = the nvlddmkm DWORD path + the test value + the concrete observable + the HYPOTHESIS prediction + the risk class (RMForcePcieConfigSave x19 SAFE, RmDisableDecompOnlyLce x9, RmClk2Enable x8; CAUTION: RMUseTc0NonCoherent, RML2MaxWaysSysmem, RMEnableQoSRunlistIntr). PART D: runbook-426.sh — the capture day scripted (prereq the STOCK sha / patch the 4.25 recv hook / restore byte-exact / initramfs plumbing / capture with the §7.2 checklist + the honest negative / regkeys the day-2 pack); no lab-side card gesture, the law-2 boundary holds** | v437a_data_twin_xrefs + v437b_regkey_cards (+JSONs) + runbook-426.sh + findings-4.37-breakday-package.md; 6 selftests reproduce the banked evidence exactly (the law 519 windows, the 4.32 counts, the 36 c.lui, T1 22/22 positions, d4d856ff x1019, the T3b ladder head); 6 instrument lessons (the idiom trap closed, cite the universe, hit positions not layouts, a zero needs its widened twin, the probe order is evidence, the cards cite they do not invent) |
| 4.38 | dual-core-provenance | **the dual-core power provenance — the reported 250 W lives in NO binary as a constant; the applied limit = runtime data parsed from the VBIOS by the closed x86 RM. SUBSTRATE: the CLOSED kernel/nvidia/nv-kernel.o_binary acquired and sha-verified (120,980,872 B, c90f58d5…, package b2e935c6… byte-exact vs the 4.28 register; outside the repo). TASK A: the EDPp writer→source map COMPLETE (34 WRITE-FIELD uses, the copy-chain trace) — every source = FIELD-COPY from runtime objects / flags {0,1,−1} / the RESET; NO power constant, NO RPC-buffer fill; the 0x660 chain named (buf[0x10] → the 0x2080A080 dispatcher → the 4×8-B table at B+0x8D9DC with the index buf[0]∈[0,3], the tail-entry cluster cited); the 0x2080A618 event = the mark-state setter (the SAME body as the RPC 0x2080d031 handler); the GET_PARAMS six-limit lane {0x104..0x118} closed (a userspace template never firmware-filled). TASK B: the x86 census — open core {240000,250000,280000} = 0/0 (the 12 real 100000 = reset/timeout/DP/SPDM; the 4.28's 14 raw-u32 = ALL decoys at insn level); CLOSED core data words = ZERO for the whole family + the v9 triple detector 0 hits; the closed 250000 ×7 = the TIME TWINS (_nv059xxxrm: the {500000,250000} clamp-hysteresis = s4/s5's structure, the deadband sub = s2, the ×1e5 imuls = the 4.34b MUL class — the same RM source compiled twice); the wattage float ladder {220..296} = HYPOTHESIS (0 refs). TASK C: the application map (VBIOS parse → the closed-x86 heap → the clamp of -pl against the runtime limitMax → NVML reports; the GSP 0x6d0 object = a re-derivation surface, NOT the host clamp) + the patch design (Lane V = the VBIOS-in-RAM patch PRIMARY, Lane R = the revised {240000,250000}-pair scanner, Lane T = the RatedTdp obj+0x3bc0 hook, Lane H demoted)** | v438a_edpp_writers + v438a2_handlers + v438a3_dispatchers + v438a4_table_writers + v438b_x86_power_sites + v438c_closed_core (+JSONs) + findings-4.38-dual-core-provenance.md; the census reproduction 17/17; the closed-core recipe = the package sha + the extract command; 5 instrument lessons (objdump-decoded immediates only — the raw u32 = decoys; movabs ≠ its low dword; the ET_REL cross-section symbol trap; the linear island walk desyncs — byte-pattern next; the stack-offset noise floor) |

| 4.40 | rop-gadget-hunt | **the ROP gadget hunt in OUR bootloader (the runtime-lane preparation): the WRITE-PRIMITIVE pair @0x100b3e+0x100b48 = a SELF-ADVANCING transfer-list gadget (ld a5,0x8(sp) → addi a6,a5,0x8 → ld a5,0x0(a5) → sd a6,0x8(sp) → sd a5,0x0(a1)), the bounds bgeu a5,a0 @0x100b3a built-in, the walk pointer self-advancing; the emulator runs the gadget 39 steps NO fault; 84 rets, 515 auipc = the runtime-lane gadget inventory; the FEAT PLM 0x00823804 = not materialized in the booter (the chain = ours to build via the memdesc 0xF800 entries)** | the gadget scan (the custom python, this pass) + tools/booter_emu.py (the 4.31 oracle); findings-4.40-rop-gadget-hunt.md |
| 4.41 | transfer-list-decoded | **the transfer loop decoded instruction by instruction (bootloader.asm): 0x100b04 = the size ld, 0x100b0a = the dest base ld, 0x100b14/18 = the source pointer init (sp+0x18 → sp+8), 0x100b3e-48 = the load/advance/store body, 0x100b5a = the slot store; the conclusion: the memdesc stack = the SOURCE, the booter transfers our data into the RM state register itself** | findings-4.41-transfer-list-decoded.md (the full re-decode + the corrections = the 4.42 pass) |
| 4.42 | transfer-list-build | **the transfer-list BUILT and PROVEN end-to-end. TASK 1 the loop 0x100aec-0x100ba6 re-decoded COMPLETE (the 4.41 six-line summary corrected on two points: the store @0x100b5a = the SLOT update, not a "success flag"; the source = the callee's OWN frame sp+0x18 = the saved args a1-a7, the walk cell @sp+0x8): the entries = FLAT u64, stride +8 fixed (addi a6,a5,0x8), NO {value,next} pairs — the 16-B hypothesis FALSIFIED; n = a0+1 = (n-2) raw + 1 signature ((last-0x16d000)&0xFFFF | magic<<56 | n<<48, the magic byte = 0x8 written by the setup @0x1022ca) + 1 rdtime timestamp (1 tick = 1 ns, 4.34); the slot-0 = the PERSISTENT count (bumped by n, the resume = (count % (cap-1))+1 @0x1022ea = remu — the ring survives reboots); the context a4 = 0x124000 (auipc 0x23+0x504), the fields {+0x488 slot, +0x490 capacity, +0x498 dest base, +0x4a0 magic}; 21 CALLERS found (v442a, the auipc imm20-SIGNED fix — lesson 4.31-L7 re-banked) = the booter's stage-event logger; the setup decoded (capacity = [desc+0x20]>>3, dest = [desc+0x28], 0x16C088 = the desc cell, the static cells = ZERO → inert without runtime setup); the memdesc capacity MEASURED: .fwsignature_ga10x = 0x1000 in OUR gsp_ga10x.bin → 512 u64 entries max (not 256). TASK 2 the {value,target} table: the premise corrected (the six limits @0x104..0x118 = the 0x2080d031 params struct, 4.38 §1.3 — the userspace-template DEAD lane; the policy object's only statically-proven field = obj+0x660, 4.20 §4); the encoding mW u32 LE (the transport + the capture triplet); E1 = params+0x108 ← 0x000445C00003A980 {limitRated 240000 kept, limitMax 280000} (the aligned u64), E2 = obj+0x660 ← 0x000445C0 (the +0x664 neighbor = UNKNOWN, the recompute = volatile — the risks named), E3 = the four-aligned-u64 template card; the targets = RUNTIME-resolved absolute (the ROP ld gadgets = the 4.40 inventory, the next work). TASK 3 the C patch: tools/booter-patch/transfer_list_memdesc.c (tl_build_payload + tl_patch_signature_memdesc, the drop-in after kernel_gsp.c:5697 portMemCopy — the driver function cited from the 610.57.04 source fetched this pass; the 0xF800 = the campaign's historical name, the byte-proven size = 4096 B, the constant ABSENT from the booter); v442f test 4/4 PASS (gcc -Wall -Wextra clean, the C dump == the python builder BYTE-EXACT e85c14d5…). TASK 4 the emulator: rdtime modeled (deterministic, 1 tick = 1 ns), --test-transfer added: 11/11 PASS (TT-A the gadget scatter in order + the slot 2→5 + [dest]+=3; TT-B the full loop n=2 on the REAL ctx 0x124000 — sig = 0802_0000_0000_0FB0 EXACT, the timestamp, the count 6800→6802; TT-C the wrap slot=cap→1 never 0; TT-D the guards capacity=0 → inert; TT-E E2E the BUILT payload drives the real loop, E1 lands at the 3 slots); the 4.31 selftest 5/5 intact. 4 instrument lessons (the imm20-SIGNED scan trap twice, the mnemonic-in-operand-regex twice, a previous pass's summary ≠ the substrate, the capacity = a measurement not a memory)** | v442a_callers + v442b_indirect + v442c_ring_globals + v442d_ring_setup + v442e_transfer_list_build (+JSONs + v442e_payload.bin) + v442f_c_payload_test.py + transfer_list_memdesc.c + booter_emu.py --test-transfer (TT battery); findings-4.42-transfer-list-build.md |
| 4.43 | power-enforcement-map | **the power path INSIDE GSP-RM mapped, hop by hop. TÂCHE 1 the VBIOS-read hypothesis FALSIFIED for the power policy: the FWSECLIC VBIOS-VERIFY report machinery exists (the report-arm block 0x11bf000-0x11c0a00, the VBIOS codes 0x27-0x2e cited per arm, the name strings @0x1E30158-0x1E30348, the variadic logger 0x1A9E624 with tp+0x88 thread-local state) BUT 0 crypto constants in the covered RM code (SHA-256 IV x8, SHA-1 IV x5, RSA e = 0 sites) — the RM REPORTS codes received from the secure domain (FWSEC/SEC2), it does not verify; 0 SPI fingerprint (no JEDEC 0x9F cluster over 3,400,949 decoded insns; RMDisableSpi = 0 xrefs under both the addi-pair and the %lo-idiom censuses); the 4.22 BIT-parser negative re-banked. STRUCTURAL DISCOVERY: static auipc pairs compose to 0x2029xxxx — OUTSIDE the file-backed LOADs; the container has PT_TLS @0x203C6000 (memsz 0x1000) + PT_LOOS @0x20000000 = a runtime region the extraction lacks (the backing = HYPOTHESIS, the region class = PROVEN); every static negative is now bounded to the file-backed LOADs. TÂCHE 2 the 0x6d0 object lifecycle PROVEN: census 17/17 + WRITE 34/34 reproduced, READ = 20; creation = allocator 0x18C373C with data descriptor 0x4190DE8 (size 0x6d0, memset, stored @state+0x4E98) + the companion via 0x18C3734/0x4190DC0 (@state+0x4EA0); the recompute 0x143fdbc writes obj+0x65c (the policy mask) from internal event 0x20809009, matches it against {0x1, 0x4} @0x1C7B320, sends the list via event 0x20809064, fills the base table obj+0x600-0x660; the evaluator 0x1446d98 (a phase 1-4 state machine on *(state+0x4EA8)) applies THE FORMULA: limit = base[obj+0x618+idx*0x10] × record[obj+0x18+idx*0x30].f18 / 100 / 1000 (mul/divu/divuw cited @0x1446f14-f1c, scalers t1=0x64 t4=0x3E8) → the output buffer 0x28 = 5 × {u32,u32} = THE PER-vPstate LIMIT TABLE (the RatedTdp response structure, 4.21 §4) stored into the a2 state object (+0x48/+0x7F8/+0x8E8/+0x958); obj+0x660 = ZERO readers in all census windows (the honest negative); 0 direct callers of all three policy functions = runtime-dispatched (the 4.16 wall). TÂCHE 3 the PMGR power framework named by its asserts: pwr devices/providers (pwrDevIdx/provNumGet), channels/relationships (PstateEstLUT xref PROVEN @0x177d59c), the boardobj policies (WORKLOAD_DIE_2X, the TGP iface fb/corePolicyRelIdx, tgt/floor/ceiling relIdx), the perf-cf softFloor controllers (clkPropTopIdx/perfCfControllerClkIdx), the VOLT module (OutputVoltage1x xref PROVEN @0x17822f8), the THERM name tables (4 raw u64 pointers @0x1E15D38/0x1E17958/0x1E22EC8/0x1E24E48); GPU_BOOST_SYNC 0x16e4f20 reads its limits via the SAME base chain as the 0x2080A080 dispatcher (*(a0+0x158)→+0x2000→*(y-0x68)); the FINAL throttle compare = runtime-bound, honestly closed. The patchable targets NAMED (all runtime-fed): the bases obj+0x600-0x660, the records obj+0x18+idx*0x30, the mask obj+0x65c, the 4x8B table B+0x8D9DC, the boost limits in state B — the 4.38 verdict now proven FROM INSIDE the firmware; the injection timing = between 0x20809064 and the evaluator, the records/mask = the more persistent targets (the recompute volatility risk re-scored). 4 instrument lessons (the 0x20000000 region bound, the tail-entry architecture x3 confirmed, the %lo-idiom census closed for strings, the capstone detail-mode trap)** | v443a_vbios_lane + v443b_edpp_consumers + v443c_enforcement_anchors (+JSONs); findings-4.43-power-enforcement-map.md |
| 4.44 | runtime-values | **FROM THE FORMULA TO THE PAYLOAD — the runtime values of the limit and the concrete write chain to the 0x6d0 object. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; the law 512/512; the census 17/17; WRITE 34/34; selftest 5/5; TT 11/11; 84 c.ret + 515 asm auipc; the v442e payload byte-identical; the v442f pair e85c14d5 reproduced). TÂCHE A1 the evaluator 0x1446d98 decoded IN FULL (v444a): 4 iterations (a4=0..3, the bound s8+0x20), the idx = the POLICY-GROUP index (the match table {0x1,0x4,0x8,0x2} @0x1C7B320 — the 4.43 "{0x1024,0x1026} @0x1C7B450" = a PIC-arithmetic slip, NOT referenced by the loop), record.f18/f14 = c.lw SIGNED with the −1 SENTINELS (f18=-1 → 0xFFFFFFFF stored = unlimited; f14=-1 → the store SKIPPED = 0), normalized u32 (slli/srli 0x20) → the product = u32×u32; the output = the 0x28 stack buffer (5 slots, slot 0 = zeros), the pairs = slots 1..4, copied (0x20) to obj2+0x50 by 0x143FAAC whose 0x18BDA10 = portMemCopy(dst,dstSize,src,srcSize) PROVEN by its overlap/alignment body; +0x48 = the arg1 vmethod(+0x1a0) result; the 4.43 "+0x8E8 store" = an ARGUMENT (c.add a2,s4 @0x1446fb6); the zero stores = {0x48,0x7F8,0x8F8,0x958,0x7C8,0x9B8}. TÂCHE A2 the recompute 0x143fdbc write set COMPLETE (v444c): {mask 0x65c, the four SoA arrays A/B/C/D @obj+0x60c/0x610/0x614/0x618 +k*0x10 ← the 0x20809064 event results, the mirror 0x64c+4k (= B), record[k].{+0x10,+0x1c}, the count obj->0x8} — and NEVER record.f14/f18 (the persistence proof for the f18 route); the match table = the SAME 4 entries (the 4.43 "{0x1,0x4}" = 2 of 4); the ×0x30 stride = 2 sites in the whole image (the t3=0x3000E13 census); the record-f14/f18 writers = NOT statically nameable (the bounded negative ±[0x143f000-0x1448000)). TÂCHE B1 the descriptors 0x4190DC0/DE8 = file-backed in the container data LOAD (@0x102BDC0/0x102BDE8) and ALL-ZERO (0x80 B dumped each) — the object born memset-zero (0x1458d08) → the descriptor route DEAD for VALUES, the seeds = pure runtime; both creation calls = 0x18C373C (the 4.43 "0x18C3734" = an arithmetic slip — that's the neighbor epilogue); 0x18C3F80 = the refcount RELEASE; the allocator calls *(desc) as a ctor (a runtime fn-ptr). TÂCHE A3 the equation NOT solvable by data today: the 1616 capture = 0 pair-solves (the 9 nonzero u32s = 4 header + 5 params banked 4.24), the recv dumps = ARMED NEVER RUN (0 files), the unit-word scan {250000,240000,250000000,240000000,100000000} = 0 in rm.elf AND container (the µW forms new) → INDECIDABLE-BY-BYTES, the U2 experiment (f18 += 1 → 252500 vs 250250 mW, the delta 2.25 W) decides. TÂCHE A4 the value TABLE computed (never guessed): the minimal mod = ×28/25 on ONE factor — the f18 route (112 percent / 1120 permille, u64 {f18,0} @obj+0x18+k*0x30, PERSISTENT, the +0x1c half = the field the recompute ZEROes = self-healing) vs the base route (0x10B07600 µW percent, u64 @obj+0x618+k*0x10, VOLATILE) — the routes EXCLUSIVE (both = 313600 mW overshoot, documented by the test itself). TÂCHE B the gadget scan re-committed (v444e): 84 c.ret byte-exact, 24 CHAINABLE (the ra-reloading epilogues, the steps {64,32,48,16,496}), 0 WORK-GADGETS (ld/addi on a-regs → ret within ±8 branch-free insns, both scan directions) → the a1-refresh NOT constructible → 1 surgical write per invocation; the scatter = inherently consecutive (slot+1, wrap to 1); the D-array chain = ONE invocation (dest=obj+0x610, a1=obj+0x618, a3=8, the list [D0,D0,P,D1,P,D2,P,D3], the P = the {B,C} clobbers named+auto-healed); the f18 chain = 4 hijack cycles (the pass-III spin = the unproven re-entry); the runtime parameter obj = *(state+0x4E98) = the capture-day input (the dest≠0 guard = the TT-D inert-loop refusal). TÂCHE C the v444 builder (the ctx @0x488 + the D list @0x500 + the f18 list @0x540) byte-exact C==python (dc0c1b40…, v444f 3/3 PASS incl. the banked e85c14d5 pair); booter_emu --test-444 = 9/9 PASS on the REAL image (TF-A the D scatter lands the 4 u64s at the exact fields + the named clobbers + the counter; TF-B the f18 = 112 at obj+0x18; TF-C the route-exclusivity negative check — the emulator caught the 313.6 W design error during development); selftest 5/5 + TT 11/11 intact; runbook-444.sh = the gated break-day (the 7/7 checks with automatic refusal: the stock sha, the payload byte-exact, the 3 emulator batteries, the C 3/3, dest≠0, ONE route, the U2-decided unit; the ACK=1 gates on u2+apply280, the refusal tested; the observables nvidia-smi/RPCRECV/dmesg; the driver-only revert). 5 instrument lessons (the summary≠substrate 3rd strike, the PIC arithmetic by code never by hand, the byte-scan≠instruction-count universe, the emulator catches SEMANTIC errors, the RVC backward decode = candidates re-verified in the emulator)** | v4440_baseline + v444a_evaluator_full + v444b_descriptors + v444c_recompute + v444d_values_table + v444e_gadget_chain + v444_transfer_list_build (+JSONs + v444_payload.bin) + v444f_c_payload_test + transfer_list_memdesc.c (tl_build_payload_444) + booter_emu.py --test-444 (TF 9/9) + runbook-444.sh; findings-4.44-runtime-values.md |
| 4.44-machine | tl444-falsified | **the TL444 inert boot: the MEMDESC LANE FALSIFIED IN VIVO — the booter rejected the patched signature memdesc with error 0x1d (≠ the 0xb of the rm.elf) DURING the Booter Load; the payload was byte-exact vs the builder (85d77114, the userland-twin diff) and dest=0 = INERT (the TT-D guard, zero writes) — the failure = the memdesc replacement itself: THE SIGNATURE MEMDESC = THE BOOTER'S VERIFICATION INPUT, not a free config buffer; the emulator proved the loop MECHANICS (11/11+9/9) but the SURFACE SEMANTICS = wrong; the {value,target} tables + the formula + the f18/base routes all stand; THE REMAINING LANES: (1) the ROP runtime (the phase III, the canary defeat — proven in silicium on THIS card): the booter DMAs the memdesc ONTO ITS OWN STACK → the overflow → the ROP (the 4.40 inventory) → the booter control — the memdesc = the VEHICLE via the OVERFLOW, the verify never runs; (2) the driver-side RM-in-RAM surfaces the GSP consumes AFTER the verify = alive, the verified surfaces = dead; the tooling lessons: the mkinitcpio kms hook embeds nvidia.ko IN THE UKI (the ritual = dkms + limine-mkinitcpio, else the boot loads the OLD module — the silent false negative), /proc/cmdline = the effective truth, the rollback cycle = 10 min proven; the machine = healthy stock 250 W** | findings-444machine-tl444-falsified.md (the machine-side session) |
| 4.44b-machine | cmpunlocker-reference | **THE PUBLIC REFERENCE FOUND (the internet research, the user directive): cmpunlocker (amoghmunikote) = the WORKING unlock of the CMP 170HX/GA100 via the SAME bug class — the BootROM loads .fwsignature BEFORE the verify (the paper Jon Pry ASU, June 2026); the driver fork 610.43.03 = the patch file acquired (imports/cmpunlocker/sec2-postbl.patch, 470 lines): the uniform fill = SEC2_POSTBL_TIMING_FILL_DWORD = 0x000004a7 = OUR "spin 0x4a7" = the canary-defeat fill, the canary magic 0xc0deca7e, the 24-DWORD ROP (the BROM gadgets 0x0cbd/0x1fbd/0x7f2f = ALL ODD < 0x8000), the refill = memdescMapInternal→fill→unmap→memdescFlushCpuCaches (the step our 4.44 missed) + the WPR_META re-point; the chain = ONE BAR0 write ×4 = the PLM registers (WPR_CFG, FBPA, WPR, FEAT) opened → the host writes the unlock via BAR0 with the STOCK signature restored; THE DELTAS FOR OUR GA104: the signature = 0x1000 (4 KB, measured) vs their 63 KB → the compact chain fits; the gadgets = our 4.40 booter inventory (the ROP = the BOOTER stack, not the BROM); the target = the f18 route (100→112 = 280 W, PERSISTANT); THE 0x1d BOOT = does NOT falsify the overflow lane (the copy happened, the verify failed on the garbage content, no chain = ours was inert) — only the free-replacement lane is dead; the operational model = the file patch + the stock driver (no DKMS/UKI) — simpler than our 4.44 path; their tools/booter_emu.py = the same instrument family (the independent convergence)** | findings-444b-cmpunlocker-reference.md + imports/cmpunlocker/sec2-postbl.patch (the machine-side session) |
| 4.45 | rop-runtime | **THE ROP RUNTIME LANE — the memdesc overflow to the booter control, the chain built and emulator-validated. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; the law 512/512; the census 17/17; READ=20/WRITE=34; selftest 5/5; TT 11/11; TF 9/9; 84 c.ret + 515 auipc re-assert; 24 chainable + 0 work-gadget re-run). TASK A the bounded NEGATIVE, quantitative (v445a/v445b): the memdesc→stack copy site = ABSENT from bootloader.asm — 44 framed functions (the max = 0x620 (main) < 0x1000: the memdesc fits NO booter stack), 166 stack writes outside frame-saves of which 0 in loops (the write×loop crossing), 10 SBI services {0x20,0x21,0x22,0x23,0x25,0x2a,0x2b,0x2d,0x2e} with no stack destination (v445b: the blocks = the WPR literals 0x1000000/0x200000 + the {addr,size} pairs of the boot block, never a stack address), the runtime-size copies = the ELF parser (the magic 0x464C457F @0x103720) to the WPR; the booter = compiled WITHOUT stack-protector (4 suspects = the block stores, inspected); ⇒ THE CONSUMER = the BOOT ROM (closed): the proof chain = the memdesc = the booter's own signature + the 0x1d reported by s_executeBooterUcode (the libos NEVER RAN) + 0 crypto in the ELF (the 4.31 verdict re-confirmed) — the paper's "booter" = the boot block (ROM + libos); the canary/the copy-verify order/the distance = INDECIDABLE-BY-BYTES, the experiments = r0 (the differential failure mode: ≠ 0x1d = the hijack confirmed, = 0x1d = the lane DEAD before coding) and r1 (the fill scale) in the runbook. TASK B the byte-proven spine: G40 = the ENTRY 0x10022A of the epilogue whose ret = 0x10023a (the execution lesson: the chain slots = the ENTRIES, NOT the scan rets — the first run crashed on this, the emulator caught it), the 0x40 steps = 8 u64s, the walk-cell = the index COMPUTED fill_len + 8·hops + 2 (= [sp+8] at the terminal entry, exact); the THREE STRUCTURAL WALLS of Lane B named: W1 = 0 work-gadget → a1 (the target) and a4 (the ctx) = the non-constructible ROM residue; W2 = the body writes [a1] FIRST = the inherent wild write; W3 = the primitive's ret = re-enters itself = the SPIN (the final state the paper observes, the 0x4a7) — accepted by design (the boot = already hijacked, the reset = the driver's path); ⇒ Lane G (the paper's ROM gadgets @0xf754/0xf76c, silicon-proven on THIS card) = the constructible lane at the ROM stage, cited not byte-verifiable; THE CENTRAL PROBLEM analyzed (the ROP write timing vs the RM allocation): 5 options — O1 the direct f18 = DEAD (the object allocated BY the RM post-boot), O2 the surviving seeds = DEAD (the birth memset, 4.44), O3 the RM/WPR patch = REFUSED (the 0xb of 4.38, the FE01 lesson), O4 the boot-params seed = DEAD (no static field feeds f18), O5 the MMIO registers (the paper's route) = THE ONLY byte-coherent SURVIVOR ⇒ the v445 chain = built for the MMIO class, the f18 = the documented timing wall. TASK C the v445 builder (the uniform fill + the spine + the terminal + the walk-cell + the ctx clone 0x488 {slot0=1,cap,dest,magic} + the list 0x500, sha 4ee1f973…, the explicit A1-A4 assumptions block, each assumption = its experiment); --test-rop = 18/18 PASS on the real image (TR-A the fill swallows the canary; TR-B the spine of the real bytes, the 0x40 steps exact; TR-C the primitive a3=1 with the stop = the RE-ENTRY (W3 demonstrated by the test); TR-D the scatter a3=3 with the ring semantics documented (the #2+ = at the slot0+1.. slots); TR-E E2E the committed payload); the new rearm_stop + stop_pred in the emulator (the re-entry and the iteration = the same pc — the address stop = insufficient, the semantic stop = the oracle); selftest 5/5 + TT 11/11 + TF 9/9 intact; runbook-445.sh = the gated day (8/8 checks, the ACK=1 on r0/r1/r2, the chaining r1 requires r0≠0x1d = the automatic refusal if the lane = dead, the rollback = the driver revert alone). 5 instrument lessons (the slots = the entries not the rets; the semantic stop > the address stop; the quantitative negative = the crossing not the count; the summary ≠ the substrate 4th time — the consumer = the ROM, identified by the dmesg stage; the explicit assumption = the test's contract)** | v445a_booter_copy + v445b_arg_provenance + v444e_gadget_chain (re-committed) + v445_rop_payload_build (+JSON + v445_rop_payload.bin) + booter_emu.py --test-rop (TR 18/18, rearm_stop/stop_pred) + runbook-445.sh; findings-4.45-rop-runtime.md |
| 4.45 | rop-runtime | **LA LANE ROP RUNTIME — le débordement du memdesc vers le contrôle du booter, la chaîne conçue et validée émulateur. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; la loi 512/512; le census 17/17; READ=20/WRITE=34; selftest 5/5; TT 11/11; TF 9/9; 84 c.ret + 515 auipc re-assert; 24 chainable + 0 work-gadget re-run). TÂCHE A le négatif borné QUANTITATIF (v445a): le site de copie memdesc→pile = ABSENT de bootloader.asm — 44 fonctions à frame (la max = 0x620 (main) < 0x1000: le memdesc n'entre dans AUCUNE pile du booter), 166 écritures pile hors frame-saves DONT 0 dans les boucles (le croisement write×loop), 10 services SBI {0x20,0x21,0x22,0x23,0x25,0x2a,0x2b,0x2d,0x2e} sans destination pile (v445b: les blocs = les littéraux WPR 0x1000000/0x200000 + les paires {addr,size} du bloc de boot, jamais une adresse pile), les copies runtime-size = le parseur ELF (la magie 0x464C457F @0x103720) vers le WPR; le booter = compilé SANS stack-protector (4 suspects = les stores de blocs, inspectés); ⇒ LE CONSOMMATEUR = le BOOT ROM (fermé): la chaîne de preuve = le memdesc = la signature du booter + le 0x1d rapporté par s_executeBooterUcode (le libos n'a JAMAIS tourné) + 0 crypto dans l'ELF (le verdict 4.31 re-confirmé) — le « booter » du paper = le bloc de boot (ROM + libos); le canari/l'ordre copie-verify/la distance = INDECIDABLE-BY-BYTES, les expériences = r0 (le mode de panne différentiel: ≠ 0x1d = le hijack confirmé, = 0x1d = la lane MORTE avant de coder) et r1 (l'échelle du fill) dans le runbook. TÂCHE B l'épine byte-proven: G40 = l'ENTRÉE 0x10022A de l'épilogue dont le ret = 0x10023a (la leçon d'exécution: les slots de chaîne = les entrées, PAS les rets du scan — le premier run a crashé dessus, l'émulateur l'a attrapé), les pas 0x40 = 8 u64, la walk-cell = l'index CALCULÉ fill_len + 8·hops + 2 (= [sp+8] à l'entrée du terminal, exact); les TROIS MURS structurels de Lane B nommés: W1 = 0 work-gadget → a1 (la cible) et a4 (le ctx) = le résidu ROM non constructible; W2 = le corps écrit [a1] D'ABORD = le write SAUVAGE inhérent; W3 = le ret du primitive = ré-entrée dans lui-même = le SPIN (l'état final que le paper observe, le 0x4a7) — accepté par le design (le boot = déjà hijacké, le reset = le chemin du driver); ⇒ Lane G (les gadgets ROM @0xf754/0xf76c du paper, silicon-prouvés sur CETTE carte) = la lane constructible au stade ROM, citée non byte-vérifiable; LE PROBLÈME CENTRAL analysé (le timing du write ROP vs l'allocation RM): 5 options — O1 le f18 direct = MORT (l'objet alloué PAR LE RM post-boot), O2 les graines survivantes = MORT (le memset de naissance, 4.44), O3 le patch RM/WPR = REFUSÉ (le 0xb de 4.38, la leçon FE01), O4 la graine boot-params = MORT (aucun champ statique ne nourrit f18), O5 les registres MMIO (la route du paper) = LE SEUL SURVIVANT byte-cohérent ⇒ la chaîne v445 = construite pour la classe MMIO, la f18 = le mur du timing documenté. TÂCHE C le builder v445 (la fill uniforme + l'épine + le terminal + la walk-cell + le ctx clone 0x488 {slot0=1,cap,dest,magic} + la liste 0x500, sha 4ee1f973…, le bloc d'assomptions A1-A4 explicite, chaque assomption = son expérience); --test-rop = 18/18 PASS sur l'image réelle (TR-A la fill avale le canari; TR-B l'épine des octets réels, les pas 0x40 exacts; TR-C le primitive a3=1 avec le stop = la RÉ-ENTRÉE (W3 démontré par le test); TR-D le scatter a3=3 avec la sémantique du ring documentée (les #2+ = aux slots slot0+1..); TR-E E2E le payload commis); le nouveau rearm_stop + stop_pred dans l'émulateur (la ré-entrée et l'itération = le même pc — le stop par adresse = insuffisant, le stop sémantique = l'oracle); selftest 5/5 + TT 11/11 + TF 9/9 intacts; runbook-445.sh = le jour gaté (8/8 checks, les ACK=1 sur r0/r1/r2, le chaînage r1 exige r0≠0x1d = le refus automatique si la lane = morte, le rollback = le revert driver seul). 5 leçons d'instrument (les slots = les entrées pas les rets; le stop sémantique > le stop par adresse; le négatif quantitatif = le croisement pas le décompte; le résumé ≠ le substrat 4e fois — le consommateur = le ROM, identifié par le stade dmesg; l'assomption explicite = le contrat du test)** | v445a_booter_copy + v445b_arg_provenance + v444e_gadget_chain (re-commis) + v445_rop_payload_build (+JSON + v445_rop_payload.bin) + booter_emu.py --test-rop (TR 18/18, rearm_stop/stop_pred) + runbook-445.sh; findings-4.45-rop-runtime.md |
| 4.46 | optimization-hunt3 | **THE OPTIMIZATION HUNT, ROUND 3 — qui nourrit la formule, les récepteurs d'événements, la région opaque 0x20000000, la surface booter. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; la loi 512/512; le census 17/17; READ=20/WRITE=34; selftest 5/5; TT 11/11; TF 9/9; TR 18/18 — le nouveau bancé 4.45; 84 c.ret exact, le sha booter ab90560bad520e65 exact). TÂCHE A LES RÉCEPTEURS TROUVÉS (la queue 4.43 #3 payée): l'espace d'événements cartographié (v446a: 28 sites, 21 IDs distincts 0x2080_8xxx-0x2080_Exxx, les clusters 0x18AB-0x18AC + 0x1BD96xx + 0x1AEA0CA + 0x1447806 DANS le module policy); le dispatcher géant @0x18AB1A0 (le switch binaire sur l'ID entrant, ~7 KB, sa config statique = la table {ptr,taille} file-backed @data 0x418FFA0 {0x10E2B34,0x1C8},{0x10E30A0,0x1F0},{0x10E231C,0x1F0}); LE HANDLER 0x20809064 = UN LOOKUP CLÉ→LIGNE PROUVÉ: la table source à (objet a0)+0x32BC8 ({u32 count @+0x32BC4; rows 0x10 = {key, A, B, D}}), le match row.w0 == la clé de groupe {1,4,8,2} pré-placée par la recompute depuis la table statique 0x1C7B320, la row copiée dans la réponse — D (w3) = obj+0x618+k*0x10 = LA BASE µW QUE LA FORMULE MULTIPLIE (la fenêtre 0x18ABCCA-D22 citée intégrale); le négatif borné: 0 store statique aux offsets exacts {+0x32BC4,+0x32BC8} (les 5 compositions voisines = d'autres objets) — le remplissage = par bloc, la lane RPC object-create (le modèle 4.37/4.38) = HYPOTHÈSE cohérente. TÂCHE B LA COUTURE STATIQUE DES RECORDS = MORTE (le négatif 4.44 §2.3 re-ouvert full-image, toutes encodages): le template statique = 0 paire {f14,f18} (NI 100 NI 1000) dans le data LOAD entier (les deux lectures de l'unité); l'idiome ×0x30 byte-exact = 6 sites full-image (les 2 bancés + 4 NOUVEAUX 0x14D913E/0x1518888/0x1A49E0C/0x1BCE294 décodés un à un = d'autres systèmes de records, AUCUN n'écrit +0x14/+0x18); la marche de pointeur addi rd,rd,0x30 = 528 sites (trop dense pour l'isolation — la forme sans matérialisation du multiplicateur); L'INIT 0x14584D8 DÉCODÉE = une CHAÎNE D'ÉVÉNEMENTS dispatcher (alloue 0x4E20 → événement 0x2080A618 via le MÊME slot +0x138 → relie 8 u32 → obj+0x668-0x688 et le byte s5+0x98|0x100 → obj+0x664 (la source du « voisin inconnu » 4.42/4.44); alloue 0xD450 → 0x2080A612 + 0x2080A618, les recopies croisées) — elle ne touche JAMAIS les records ⇒ les records = remplis par la lane runtime (RPC/ctrl object-create, HYPOTHÈSE cohérente), la dernière couture statique de la route f18 = fermée. TÂCHE C LA RÉGION 0x20000000 QUANTIFIÉE (la queue 4.43 #2 payée): 71,541 compositions (71,494 auipc + 47 lui) → 65,060 cibles distinctes dans [0x2000000C, 0x203C5AEF] = 3.95 MB bornés par le PT_TLS @0x203C6000 — le vrai espace rodata/config du RM référencé 71K fois; les sites cités 4.43 §1.2 reproduits par assert dur; le backing: le seul candidat avec la place = le record GFW rm.bindata.bin (0x3d8a000 = 64.4 MB, flags 0) — pas de mapping identitaire, la sonde NUL-préambule (8 VAs cités, balayage de tous les offsets K) max = 3/8 = le bruit, les hits u32 dans l'intervalle = SOUS l'espérance aléatoire (28.7K observés vs ~30.5K attendus) ⇒ le backing = compressé/chiffré, INDECIDABLE-BY-BYTES; LA CONSÉQUENCE CAMPAGNE: le sol 4.36 = RE-CADRÉ — 71,541 refs pointent dans une région opaque, tout négatif statique à venir porte le double bornage (les 2 LOADs file-backed + le rodata opaque). TÂCHE D LA SURFACE BOOTER (première mesure): 0 knob (la règle lane A sur le LOAD entier = 0 valeur round — le booter = un chargeur sans constante de politique, le µs-ladder = RM-only); le SBI = la face appel bankée, la face réception = le ROM (le négatif borné inchangé 4.31); LA CELLULE DESC 0x16C088 = ORPHELINE côté booter (6 compositions 0x16C = les cellules voisines: le tableau logger 0x16C230-0x16C268 écrit par le setup sd a0,0(s6) @0x101E0C, 0x16C340, 0x16CF88 — personne n'écrit la desc) ⇒ LA LANE « DMEM-TAIL » NOMMÉE: si la queue DMEM (0x16C000+, dans le LOAD après le code) échappe à la couverture signature memdesc (le UNDECIDABLE 4.30 jamais tranché), le driver arme le ring au staging — la boucle de transfert écrirait les {valeur,cible} LÉGITIMEMENT SANS DÉBORDEMENT NI ROP (l'optimisation maximale de la lane 4.45); l'expérience qui décide = le dump post-boot stock de {0x16C088,+0x20,+0x28} + le test de couverture signature de la queue (le runbook 4.44-machine, le même jour machine). 5 leçons d'instrument (auipc 0x17 ≠ lui 0x37 — l'assert sur le site cité a attrapé le bug avant le verdict; le split immédiat S-type ≠ I-type + le seuil de signe 0x800; l'espérance aléatoire AVANT de crier structure — les 28.7K hits = sous le hasard; la sonde NUL-préambule = l'outil de rejet de mapping; c.li ne porte pas 0x30 — imm6 signé)** | v446a_events + v446b_records + v446c_region2000 + v446d_booter_surface (+JSONs); findings-4.46-optimization-hunt3.md |
| 4.46 | optimization-hunt3 | **LA CHASSE D'OPTIMISATION, RONDE 3 — qui nourrit la formule, les récepteurs d'événements, la région opaque 0x20000000, la surface booter. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; la loi 512/512; le census 17/17; READ=20/WRITE=34; selftest 5/5; TT 11/11; TF 9/9; TR 18/18 — le nouveau bancé 4.45; 84 c.ret exact, le sha booter ab90560bad520e65 exact). TÂCHE A LES RÉCEPTEURS TROUVÉS (la queue 4.43 #3 payée): l'espace d'événements cartographié (v446a: 28 sites, 21 IDs distincts 0x2080_8xxx-0x2080_Exxx, les clusters 0x18AB-0x18AC + 0x1BD96xx + 0x1AEA0CA + 0x1447806 DANS le module policy); le dispatcher géant @0x18AB1A0 (le switch binaire sur l'ID entrant, ~7 KB, sa config statique = la table {ptr,taille} file-backed @data 0x418FFA0 {0x10E2B34,0x1C8},{0x10E30A0,0x1F0},{0x10E231C,0x1F0}); LE HANDLER 0x20809064 = UN LOOKUP CLÉ→LIGNE PROUVÉ: la table source à (objet a0)+0x32BC8 ({u32 count @+0x32BC4; rows 0x10 = {key, A, B, D}}), le match row.w0 == la clé de groupe {1,4,8,2} pré-placée par la recompute depuis la table statique 0x1C7B320, la row copiée dans la réponse — D (w3) = obj+0x618+k*0x10 = LA BASE µW QUE LA FORMULE MULTIPLIE (la fenêtre 0x18ABCCA-D22 citée intégrale); le négatif borné: 0 store statique aux offsets exacts {+0x32BC4,+0x32BC8} (les 5 compositions voisines = d'autres objets) — le remplissage = par bloc, la lane RPC object-create (le modèle 4.37/4.38) = HYPOTHÈSE cohérente. TÂCHE B LA COUTURE STATIQUE DES RECORDS = MORTE (le négatif 4.44 §2.3 re-ouvert full-image, toutes encodages): le template statique = 0 paire {f14,f18} (NI 100 NI 1000) dans le data LOAD entier (les deux lectures de l'unité); l'idiome ×0x30 byte-exact = 6 sites full-image (les 2 bancés + 4 NOUVEAUX 0x14D913E/0x1518888/0x1A49E0C/0x1BCE294 décodés un à un = d'autres systèmes de records, AUCUN n'écrit +0x14/+0x18); la marche de pointeur addi rd,rd,0x30 = 528 sites (trop dense pour l'isolation — la forme sans matérialisation du multiplicateur); L'INIT 0x14584D8 DÉCODÉE = une CHAÎNE D'ÉVÉNEMENTS dispatcher (alloue 0x4E20 → événement 0x2080A618 via le MÊME slot +0x138 → relie 8 u32 → obj+0x668-0x688 et le byte s5+0x98|0x100 → obj+0x664 (la source du « voisin inconnu » 4.42/4.44); alloue 0xD450 → 0x2080A612 + 0x2080A618, les recopies croisées) — elle ne touche JAMAIS les records ⇒ les records = remplis par la lane runtime (RPC/ctrl object-create, HYPOTHÈSE cohérente), la dernière couture statique de la route f18 = fermée. TÂCHE C LA RÉGION 0x20000000 QUANTIFIÉE (la queue 4.43 #2 payée): 71,541 compositions (71,494 auipc + 47 lui) → 65,060 cibles distinctes dans [0x2000000C, 0x203C5AEF] = 3.95 MB bornés par le PT_TLS @0x203C6000 — le vrai espace rodata/config du RM référencé 71K fois; les sites cités 4.43 §1.2 reproduits par assert dur; le backing: le seul candidat avec la place = le record GFW rm.bindata.bin (0x3d8a000 = 64.4 MB, flags 0) — pas de mapping identitaire, la sonde NUL-préambule (8 VAs cités, balayage de tous les offsets K) max = 3/8 = le bruit, les hits u32 dans l'intervalle = SOUS l'espérance aléatoire (28.7K observés vs ~30.5K attendus) ⇒ le backing = compressé/chiffré, INDECIDABLE-BY-BYTES; LA CONSÉQUENCE CAMPAGNE: le sol 4.36 = RE-CADRÉ — 71,541 refs pointent dans une région opaque, tout négatif statique à venir porte le double bornage (les 2 LOADs file-backed + le rodata opaque). TÂCHE D LA SURFACE BOOTER (première mesure): 0 knob (la règle lane A sur le LOAD entier = 0 valeur round — le booter = un chargeur sans constante de politique, le µs-ladder = RM-only); le SBI = la face appel bankée, la face réception = le ROM (le négatif borné inchangé 4.31); LA CELLULE DESC 0x16C088 = ORPHELINE côté booter (6 compositions 0x16C = les cellules voisines: le tableau logger 0x16C230-0x16C268 écrit par le setup sd a0,0(s6) @0x101E0C, 0x16C340, 0x16CF88 — personne n'écrit la desc) ⇒ LA LANE « DMEM-TAIL » NOMMÉE: si la queue DMEM (0x16C000+, dans le LOAD après le code) échappe à la couverture signature memdesc (le UNDECIDABLE 4.30 jamais tranché), le driver arme le ring au staging — la boucle de transfert écrirait les {valeur,cible} LÉGITIMEMENT SANS DÉBORDEMENT NI ROP (l'optimisation maximale de la lane 4.45); l'expérience qui décide = le dump post-boot stock de {0x16C088,+0x20,+0x28} + le test de couverture signature de la queue (le runbook 4.44-machine, le même jour machine). 5 leçons d'instrument (auipc 0x17 ≠ lui 0x37 — l'assert sur le site cité a attrapé le bug avant le verdict; le split immédiat S-type ≠ I-type + le seuil de signe 0x800; l'espérance aléatoire AVANT de crier structure — les 28.7K hits = sous le hasard; la sonde NUL-préambule = l'outil de rejet de mapping; c.li ne porte pas 0x30 — imm6 signé)** | v446a_events + v446b_records + v446c_region2000 + v446d_booter_surface (+JSONs); findings-4.46-optimization-hunt3.md |
| 4.47 | bandwidth-hunt | **THE BANDWIDTH HUNT — la référence 0xSero décodée, la carte à deux piliers, les cartes knobs MCLK/L2. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; la loi 512/512; selftest 5/5; TT 11/11; TF 9/9; TR 18/18; map 670 régions). LA RÉFÉRENCE DÉCODÉE (oembed): « 555 / 608 GB/s, within 8% of the memory bandwidth ceiling » = 91.3 % du plafond THÉORIQUE par des NOYAUX logiciels (kernels STREAM-class) sur un GPU IA Intel — PAS un hack firmware, PAS un plafond levé ⇒ la leçon = la MÉTRIQUE (% du plafond jamais mesurée dans la campagne — absence PROUVÉE) et le SPLIT: Pilier A = LEVER le plafond (notre territoire), Pilier B = SATURER le plafond (les noyaux, le jour machine); la cible 91 % de 448 GB/s = ~408 effectifs. PILIER A — LA CARTE: A1 la lane LACT mclk = appliquée-preuve mais gain NON-démontré (vram-ab: +200 → 6801→6901 MHz confirmés, glmark2 ne voit RIEN — la leçon: le juge doit être bandwidth-bound); A2 la couche timings VBIOS (gx4/gx5 bancé): 65 records × 76 B, 18 champs nommés, top-bin rc=78/rfc=210/ras=52/rp=26/cl=24, LA PREUVE VENDOR DE MARGE (le build LHR a SERRÉ les records Hynix mêmes bins: rc 76→70, rfc 210→175, faw 28→20, rrd 7→5) — le mur CERT20 ferme le FLASH, la route ouverte = le RETARGET RUNTIME (les copies parsées dans l'état RM = l'analogue f18 exact via la machinerie 4.42/4.45/4.46-DMEM-tail); A3 LES CARTES KNOBS NOUVELLES (v447a/b): la LEÇON D'INSTRUMENT d'abord (les noms regkey = SOUS-CHAÎNES de runs concaténés — le premier run a rendu occ=0 partout en égalité exacte, l'instrument corrigé matche la contenance + le VA du premier octet; DRAMCLK = 7 chars sous le seuil ≥8 — sonde séparée à seuil 4); 11 REGKEY-CANDIDATES au pattern lookup-by-name (RmDisableDecompOnlyLce ×9, RMUseTc0NonCoherent ×7, RmIsoHubMCLKSwitch ×2, RMClkSlowDown ×2, RmClkMclkProg, RMClkVfOverride, RMClkSwitchWithinMargin, RMReportMclkSwitchFbStopTime, RmMClkP5LinkTrainingWckStopClks, RmOptp2LowerMclk, RmLpwrSysClkSd, RmPerfCfPmSensorOverrides); RML2MaxWaysSysmem ×4 = CITED en SWITCH-DE-POINTEURS (4 comparaisons bgeu/bltu @0x130697e-0x1306aec contre des VAs de noms internés consécutifs — une consommation RÉELLE de mécanisme ≠ strcmp); MCLK_LIMIT = DATA-CITED ×4 (les enregistrements u64 {name-ptr,…} @0xe15c78/0xe17918/0xe22e88/0xe24e08 — les tables de limites perf/clock portent le nom interné); la fenêtre RmClkMclkProg bancée (name→a1, type-tag a0=3, call ~0x1A9E65C, store `sb zero,0x38(s8)` @0x10e8f7e); les 3 strings soft-floor (NV2080_CTRL_CLK_CLK_DOMAIN_INDEX/softFloor) = PRESENCE (la machinerie clk-domain compile dans NOTRE image, le mécanisme xref non-PIC non résolu); A4 la surface booter = 0 knob (4.46 re-cité). LE RUNBOOK-447 (le jour machine): §0 les gardes + l'état (le sha gsp relevé, RIEN de firmware modifié — journée STOCK); §1 Pilier B LA BATTERIE % PLAFOND (D2D copy / read-only / write-only, 2 GiB, 3 reps, médianes, GB/s + % de 448 — le harnais sondé nvbandwidth→torch→nvcc, sans harnais = INDECIDABLE logé); §2 Pilier A le sweep LACT mclk {0,+500,+1000,+1500} JUGÉ par la batterie §1 (la leçon vram-ab appliquée) + la restauration CONFIRMÉE; §3 le pack regkey = UNE clé dégagée (RmClk2Enable=1, une boot un compteur) + les REFUS NOMMÉS (RMClkVfOverride = le précédent 4.23 de corruption de table clock; RMUseTc0NonCoherent = le risque cohérence; RML2MaxWaysSysmem = observe-only jusqu'à ce que la TÂCHE C nomme le store); §4 le rollback checklist. LA GROSSE TÂCHE (MISSION 4.47 continuation, armée): TÂCHE A = le jour runbook-447 (le verdict ~/bandwidth-447/verdict-*.json); TÂCHE B = la chasse f18-analog des timings: OÙ vivent les records parsés au runtime (l'expérience décisive = le dump DMEM post-boot cherché pour les octets du record top-bin — la méthode 4.44-machine memdesc; si DMEM-state → le payload {valeur,cible} timing = TÂCHE D; si FB-Falcon/MC direct → la lane meurt honnêtement); TÂCHE C = nommer le store de RML2MaxWaysSysmem + la sémantique du call RmClkMclkProg (~0x1A9E65C); TÂCHE D (gated B) = le payload timing-record sur la machinerie prouvée + --test-timings. LEDGER: PROUVÉ (la référence, les batteries reproduites, les cartes v447a/b, les 4 records MCLK_LIMIT, le switch-pointeurs L2, la fenêtre MclkProg); HYPOTHÈSE (le gain +5-10 % OC — folklore jamais jugé par une charge bandwidth; le gain timing-runtime; la lecture switch-interne); INDECIDABLE-BY-BYTES (où vivent les records parsés — le dump décide; le % plafond stock — la batterie décide); REFUSÉS-PAR-CARTE (RMClkVfOverride, RMUseTc0NonCoherent, les tours aveugles time-math 4.32/4.35). La branche = pass/4.47-bandwidth-hunt empilée sur 4.46; le push attend un PAT frais (aucun token dans la session — l'hygiène tenue)** | v447a_clkknobs + v447b_knob_contexts (+JSONs); tools/edpp/runbook-447.sh; findings-4.47-bandwidth-hunt.md |
| 4.47 | bandwidth-hunt | **LA CHASSE BANDE PASSANTE — la référence 0xSero décodée, la carte à deux piliers, les cartes knobs MCLK/L2. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; la loi 512/512; selftest 5/5; TT 11/11; TF 9/9; TR 18/18; map 670 régions). LA RÉFÉRENCE DÉCODÉE (oembed): « 555 / 608 GB/s, within 8% of the memory bandwidth ceiling » = 91.3 % du plafond THÉORIQUE par des NOYAUX logiciels (kernels STREAM-class) sur un GPU IA Intel — PAS un hack firmware, PAS un plafond levé ⇒ la leçon = la MÉTRIQUE (% du plafond jamais mesurée dans la campagne — absence PROUVÉE) et le SPLIT: Pilier A = LEVER le plafond (notre territoire), Pilier B = SATURER le plafond (les noyaux, le jour machine); la cible 91 % de 448 GB/s = ~408 effectifs. PILIER A — LA CARTE: A1 la lane LACT mclk = appliquée-preuve mais gain NON-démontré (vram-ab: +200 → 6801→6901 MHz confirmés, glmark2 ne voit RIEN — la leçon: le juge doit être bandwidth-bound); A2 la couche timings VBIOS (gx4/gx5 bancé): 65 records × 76 B, 18 champs nommés, top-bin rc=78/rfc=210/ras=52/rp=26/cl=24, LA PREUVE VENDOR DE MARGE (le build LHR a SERRÉ les records Hynix mêmes bins: rc 76→70, rfc 210→175, faw 28→20, rrd 7→5) — le mur CERT20 ferme le FLASH, la route ouverte = le RETARGET RUNTIME (les copies parsées dans l'état RM = l'analogue f18 exact via la machinerie 4.42/4.45/4.46-DMEM-tail); A3 LES CARTES KNOBS NOUVELLES (v447a/b): la LEÇON D'INSTRUMENT d'abord (les noms regkey = SOUS-CHAÎNES de runs concaténés — le premier run a rendu occ=0 partout en égalité exacte, l'instrument corrigé matche la contenance + le VA du premier octet; DRAMCLK = 7 chars sous le seuil ≥8 — sonde séparée à seuil 4); 11 REGKEY-CANDIDATES au pattern lookup-by-name (RmDisableDecompOnlyLce ×9, RMUseTc0NonCoherent ×7, RmIsoHubMCLKSwitch ×2, RMClkSlowDown ×2, RmClkMclkProg, RMClkVfOverride, RMClkSwitchWithinMargin, RMReportMclkSwitchFbStopTime, RmMClkP5LinkTrainingWckStopClks, RmOptp2LowerMclk, RmLpwrSysClkSd, RmPerfCfPmSensorOverrides); RML2MaxWaysSysmem ×4 = CITED en SWITCH-DE-POINTEURS (4 comparaisons bgeu/bltu @0x130697e-0x1306aec contre des VAs de noms internés consécutifs — une consommation RÉELLE de mécanisme ≠ strcmp); MCLK_LIMIT = DATA-CITED ×4 (les enregistrements u64 {name-ptr,…} @0xe15c78/0xe17918/0xe22e88/0xe24e08 — les tables de limites perf/clock portent le nom interné); la fenêtre RmClkMclkProg bancée (name→a1, type-tag a0=3, call ~0x1A9E65C, store `sb zero,0x38(s8)` @0x10e8f7e); les 3 strings soft-floor (NV2080_CTRL_CLK_CLK_DOMAIN_INDEX/softFloor) = PRESENCE (la machinerie clk-domain compile dans NOTRE image, le mécanisme xref non-PIC non résolu); A4 la surface booter = 0 knob (4.46 re-cité). LE RUNBOOK-447 (le jour machine): §0 les gardes + l'état (le sha gsp relevé, RIEN de firmware modifié — journée STOCK); §1 Pilier B LA BATTERIE % PLAFOND (D2D copy / read-only / write-only, 2 GiB, 3 reps, médianes, GB/s + % de 448 — le harnais sondé nvbandwidth→torch→nvcc, sans harnais = INDECIDABLE logé); §2 Pilier A le sweep LACT mclk {0,+500,+1000,+1500} JUGÉ par la batterie §1 (la leçon vram-ab appliquée) + la restauration CONFIRMÉE; §3 le pack regkey = UNE clé dégagée (RmClk2Enable=1, une boot un compteur) + les REFUS NOMMÉS (RMClkVfOverride = le précédent 4.23 de corruption de table clock; RMUseTc0NonCoherent = le risque cohérence; RML2MaxWaysSysmem = observe-only jusqu'à ce que la TÂCHE C nomme le store); §4 le rollback checklist. LA GROSSE TÂCHE (MISSION 4.47 continuation, armée): TÂCHE A = le jour runbook-447 (le verdict ~/bandwidth-447/verdict-*.json); TÂCHE B = la chasse f18-analog des timings: OÙ vivent les records parsés au runtime (l'expérience décisive = le dump DMEM post-boot cherché pour les octets du record top-bin — la méthode 4.44-machine memdesc; si DMEM-state → le payload {valeur,cible} timing = TÂCHE D; si FB-Falcon/MC direct → la lane meurt honnêtement); TÂCHE C = nommer le store de RML2MaxWaysSysmem + la sémantique du call RmClkMclkProg (~0x1A9E65C); TÂCHE D (gated B) = le payload timing-record sur la machinerie prouvée + --test-timings. LEDGER: PROUVÉ (la référence, les batteries reproduites, les cartes v447a/b, les 4 records MCLK_LIMIT, le switch-pointeurs L2, la fenêtre MclkProg); HYPOTHÈSE (le gain +5-10 % OC — folklore jamais jugé par une charge bandwidth; le gain timing-runtime; la lecture switch-interne); INDECIDABLE-BY-BYTES (où vivent les records parsés — le dump décide; le % plafond stock — la batterie décide); REFUSÉS-PAR-CARTE (RMClkVfOverride, RMUseTc0NonCoherent, les tours aveugles time-math 4.32/4.35). La branche = pass/4.47-bandwidth-hunt empilée sur 4.46; le push attend un PAT frais (aucun token dans la session — l'hygiène tenue)** | v447a_clkknobs + v447b_knob_contexts (+JSONs); tools/edpp/runbook-447.sh; findings-4.47-bandwidth-hunt.md |
| 4.48 | trace-api | **THE TRACE API DECODED — the 61,339-call discovery, les cartes knobs 4.47 re-cadrées, la région opaque consommée prouvée, le census timing. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; la loi 512/512; map 670; selftest 5/5; TT 11/11; TF 9/9; TR 18/18). LA DÉCOUVERTE: les DEUX fenêtres « lookup-by-name knob » 4.47 appellent EXACTEMENT 0x1A9E624 (byte-exact aux deux sites); le census des couples auipc ra+jaalr ra aux DEUX alignements 2 octets (le code compressé — le scan 4-byte-only rate tous les sites): 173,433 paires → 6,941 cibles distinctes, 0x1A9E624 = 61,339 sites (35.4 %) = LA FONCTION LA PLUS APPELÉE DU RM (facteur 1.7 sur la #2 = 0x1B4DA4C à 35,225 — les blocs denses appellent les DEUX EN SÉQUENCE: push puis act). LE CORPS DÉCODÉ (204 B [0x1A9E624,0x1A9E6F0), le c.jr ra @0x1A9E6C2 = return mi-fonction, la queue froide 0x1A9E6C4-EE appartient à la fonction; toutes les cibles de branches résolues par décodeur RVC manuel cross-checké 4/4 — la première passe avait perdu imm[5]@hw[2], capstone avait raison): l'anneau PAR-TÂCHE {count @tp+T+0x88, cap @+0x90, base @+0x98, flag @+0xA0}; count==0 ou base==0 → return silencieux; push de N=a0+1 mots en slots de 8 (curseur = base+count·8 re-calculé par itération): i<a0-1 = les args BRUTS (a1..a7); i==a0-1 = le MOT PACKÉ {N<<48 | flag<<56 | (arg[a0-1]−0x20000038)&0xFFFF} (la queue froide); i==a0 = RDTIME — l'horodatage 1 ns (la preuve 4.34 réutilisée); wrap→1 à cap JAMAIS 0 (l'empreinte exacte du compteur booter 4.42); [base] += N après le push. LA CLASSIFICATION: la résolution des labels a1 = les fragments de messages de trace du RM ('Read'/'Write'/'disabling'/'ASSERTED'/'MISSIONERR'×22/'LATENTERR'×19, les noms de fonctions inforom/gpio, ET LES NOMS REGKEY EUX-MÊMES: RMPcieLinkSpeed ×46, RmIsoHubMCLKSwitch, RMROPL2FuseMaskForFModel, RMFermiBigPageSize, RmEngineContextSwitchTimeoutUs, RmSramVminCheckIgnore...) ⇒ 0x1A9E624 = LE LOGGER DE TRACE/DEBUG PAR-TÂCHE DU RM (l'analogue RM du logger booter 4.42): tag a0 = varargs+1 ({1:33,515, 2:22,321, 3:3,058, 4:629}), le mot packé = l'ID de format-string + le compte d'args + le flag, PAS UN SETTER DE KNOB. LES CARTES 4.47 RE-CADRÉES: RmClkMprog = un push de trace (a0=3: le nom + le byte valeur lbu 0x34(s8) + l'id + rdtime), le byte valeur n'a AUCUN site d'écriture dans le caller borné 222 B ni dans ±0x400 (la valeur vient d'un frame externe — le parse réel est ailleurs); RML2MaxWaysSysmem = le dispatch de NOM (les compares bgeu/bltu s5 contre les VAs internés, le nom @0x13063c4), le nom matché copié en buffer local, les messages de trace par branche (formats opaques distincts 0x202B42F0/0x202B5B05-family), les gardes c.ebreak — AUCUN store d'un ways-count visible ⇒ le store reste non-nommé (la TÂCHE C 4.47 reste OUVERTE, re-cadrée hors de la fenêtre trace/switch); le pack SAFE day-1 INCHANGÉ (RmClk2Enable + le sweep mclk jugé par la batterie ne dépendaient pas de la fenêtre). LA RÉGION OPAQUE: la base de packing 0x20000038 = 0x2C octets du début de la région 0x20000000 (4.46) et 37,031 sites de trace passent des pointeurs dedans (les format strings) ⇒ le RÔLE de la région = PROUVÉ (le rodata du RM — la table de strings/format du trace d'abord), le mapping byte-level reste INDECIDABLE (le bornage 4.46 tient). LA CHASSE TIMING (la moitié statique de la TÂCHE B): le census stride-0x4C = 349 addi x,x,0x4c + 185 constantes li-76 (c.li ne porte pas 76) + 15 mul-par-76 — le stride 76 trop commun pour isoler LE walker statiquement (INDECIDABLE-BY-BYTES, les candidats + voisinages bancés); pas de string MemTiming/TimingTable; DRAMCLK re-cité @0x1E34B20; L'EXPÉRIENCE DÉCISIVE EST MAINTENANT BON MARCHÉ: les EMPREINTES DMEM construites (v448c → dmem_fingerprints: les vecteurs gx5 top-bin {78,210,52,26,24} launch et {70,175,44,20,5} LHR en u8/u16le/u32le + la paire (rc,rfc)), le runbook-447 a gagné une §5 (le dump DMEM post-boot cherché pour les empreintes — partageable avec le jour machine 4.44/4.45); hit → la lane f18-analog timing OUVRE (TÂCHE D), pas de hit → la lane ferme honnêtement. LEDGER: PROUVÉ (l'identité de cible aux 2 fenêtres, le census 61,339 + la distribution, le corps API avec cross-check 4/4, la résolution des labels, la structure L2, le corps partenaire — 0x8b/0xa8 = le pattern __LINE__, le census stride, le round-trip empreinte); HYPOTHÈSE (le partenaire 0x1B4DA4C = lock/owner-tracking — le store @obj+0xA0 + le test de récursion; tag = varargs+1; la région opaque = la table de strings du trace); INDECIDABLE-BY-BYTES (l'offset TLS vrai T — relocation link-time; le site d'écriture du byte valeur MclkProg; le store L2; quel walker 0x4C parse les timings; où vivent les records parsés — le dump §5 décide); REFUSÉ: rien — rien tourné, rien flashé. Leçons d'instrument: le scan auipc+jaalr exige les DEUX alignements 2-octets; la vérif rs1 du jalr lit les bits [19:15] (pas [24:20]); l'épargne d'arg casse la marche TLS (ne matcher que imm(rn) avec rn en base); le décodeur RVC c.beqz porte imm[5] à hw[2]; les sections froides vivent APRÈS le premier ret — les bornes prologue/ret sous-estiment; c.addi16sp s'imprime c.addi sp,-N chez capstone)** | v448a_trace_api + v448b_trace_world + v448c_stride_timing (+JSONs); tools/edpp/runbook-447.sh §5; findings-4.48-trace-api.md |
| 4.48 | trace-api | **L'API DE TRACE DÉCODÉE — la découverte des 61,339 appels, les cartes knobs 4.47 re-cadrées, la région opaque consommée prouvée, le census timing. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; la loi 512/512; map 670; selftest 5/5; TT 11/11; TF 9/9; TR 18/18). LA DÉCOUVERTE: les DEUX fenêtres « lookup-by-name knob » 4.47 appellent EXACTEMENT 0x1A9E624 (byte-exact aux deux sites); le census des couples auipc ra+jaalr ra aux DEUX alignements 2 octets (le code compressé — le scan 4-byte-only rate tous les sites): 173,433 paires → 6,941 cibles distinctes, 0x1A9E624 = 61,339 sites (35.4 %) = LA FONCTION LA PLUS APPELÉE DU RM (facteur 1.7 sur la #2 = 0x1B4DA4C à 35,225 — les blocs denses appellent les DEUX EN SÉQUENCE: push puis act). LE CORPS DÉCODÉ (204 B [0x1A9E624,0x1A9E6F0), le c.jr ra @0x1A9E6C2 = return mi-fonction, la queue froide 0x1A9E6C4-EE appartient à la fonction; toutes les cibles de branches résolues par décodeur RVC manuel cross-checké 4/4 — la première passe avait perdu imm[5]@hw[2], capstone avait raison): l'anneau PAR-TÂCHE {count @tp+T+0x88, cap @+0x90, base @+0x98, flag @+0xA0}; count==0 ou base==0 → return silencieux; push de N=a0+1 mots en slots de 8 (curseur = base+count·8 re-calculé par itération): i<a0-1 = les args BRUTS (a1..a7); i==a0-1 = le MOT PACKÉ {N<<48 | flag<<56 | (arg[a0-1]−0x20000038)&0xFFFF} (la queue froide); i==a0 = RDTIME — l'horodatage 1 ns (la preuve 4.34 réutilisée); wrap→1 à cap JAMAIS 0 (l'empreinte exacte du compteur booter 4.42); [base] += N après le push. LA CLASSIFICATION: la résolution des labels a1 = les fragments de messages de trace du RM ('Read'/'Write'/'disabling'/'ASSERTED'/'MISSIONERR'×22/'LATENTERR'×19, les noms de fonctions inforom/gpio, ET LES NOMS REGKEY EUX-MÊMES: RMPcieLinkSpeed ×46, RmIsoHubMCLKSwitch, RMROPL2FuseMaskForFModel, RMFermiBigPageSize, RmEngineContextSwitchTimeoutUs, RmSramVminCheckIgnore...) ⇒ 0x1A9E624 = LE LOGGER DE TRACE/DEBUG PAR-TÂCHE DU RM (l'analogue RM du logger booter 4.42): tag a0 = varargs+1 ({1:33,515, 2:22,321, 3:3,058, 4:629}), le mot packé = l'ID de format-string + le compte d'args + le flag, PAS UN SETTER DE KNOB. LES CARTES 4.47 RE-CADRÉES: RmClkMprog = un push de trace (a0=3: le nom + le byte valeur lbu 0x34(s8) + l'id + rdtime), le byte valeur n'a AUCUN site d'écriture dans le caller borné 222 B ni dans ±0x400 (la valeur vient d'un frame externe — le parse réel est ailleurs); RML2MaxWaysSysmem = le dispatch de NOM (les compares bgeu/bltu s5 contre les VAs internés, le nom @0x13063c4), le nom matché copié en buffer local, les messages de trace par branche (formats opaques distincts 0x202B42F0/0x202B5B05-family), les gardes c.ebreak — AUCUN store d'un ways-count visible ⇒ le store reste non-nommé (la TÂCHE C 4.47 reste OUVERTE, re-cadrée hors de la fenêtre trace/switch); le pack SAFE day-1 INCHANGÉ (RmClk2Enable + le sweep mclk jugé par la batterie ne dépendaient pas de la fenêtre). LA RÉGION OPAQUE: la base de packing 0x20000038 = 0x2C octets du début de la région 0x20000000 (4.46) et 37,031 sites de trace passent des pointeurs dedans (les format strings) ⇒ le RÔLE de la région = PROUVÉ (le rodata du RM — la table de strings/format du trace d'abord), le mapping byte-level reste INDECIDABLE (le bornage 4.46 tient). LA CHASSE TIMING (la moitié statique de la TÂCHE B): le census stride-0x4C = 349 addi x,x,0x4c + 185 constantes li-76 (c.li ne porte pas 76) + 15 mul-par-76 — le stride 76 trop commun pour isoler LE walker statiquement (INDECIDABLE-BY-BYTES, les candidats + voisinages bancés); pas de string MemTiming/TimingTable; DRAMCLK re-cité @0x1E34B20; L'EXPÉRIENCE DÉCISIVE EST MAINTENANT BON MARCHÉ: les EMPREINTES DMEM construites (v448c → dmem_fingerprints: les vecteurs gx5 top-bin {78,210,52,26,24} launch et {70,175,44,20,5} LHR en u8/u16le/u32le + la paire (rc,rfc)), le runbook-447 a gagné une §5 (le dump DMEM post-boot cherché pour les empreintes — partageable avec le jour machine 4.44/4.45); hit → la lane f18-analog timing OUVRE (TÂCHE D), pas de hit → la lane ferme honnêtement. LEDGER: PROUVÉ (l'identité de cible aux 2 fenêtres, le census 61,339 + la distribution, le corps API avec cross-check 4/4, la résolution des labels, la structure L2, le corps partenaire — 0x8b/0xa8 = le pattern __LINE__, le census stride, le round-trip empreinte); HYPOTHÈSE (le partenaire 0x1B4DA4C = lock/owner-tracking — le store @obj+0xA0 + le test de récursion; tag = varargs+1; la région opaque = la table de strings du trace); INDECIDABLE-BY-BYTES (l'offset TLS vrai T — relocation link-time; le site d'écriture du byte valeur MclkProg; le store L2; quel walker 0x4C parse les timings; où vivent les records parsés — le dump §5 décide); REFUSÉ: rien — rien tourné, rien flashé. Leçons d'instrument: le scan auipc+jaalr exige les DEUX alignements 2-octets; la vérif rs1 du jalr lit les bits [19:15] (pas [24:20]); l'épargne d'arg casse la marche TLS (ne matcher que imm(rn) avec rn en base); le décodeur RVC c.beqz porte imm[5] à hw[2]; les sections froides vivent APRÈS le premier ret — les bornes prologue/ret sous-estiment; c.addi16sp s'imprime c.addi sp,-N chez capstone)** | v448a_trace_api + v448b_trace_world + v448c_stride_timing (+JSONs); tools/edpp/runbook-447.sh §5; findings-4.48-trace-api.md |
<<<<<<< HEAD
| 4.49 | knob-store | **THE L2 KNOB STORE NAMED END-TO-END — the TÂCHE C answer, la chasse timing s'épuise statiquement. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; la loi 512/512; les totaux stride 4.48 re-dérivés 349/185/15 exact). LA CHAÎNE RML2MaxWaysSysmem PROUVÉE: la fonction d'ingestion @0x1307AFC (s3=a1=la base config; la séquence par clé @0x1307B20-0x1307F1A matérialise chaque nom interné du bloc FB/L2 — GC6_CTX_HDR…RmEnableL2CohErrorIntr ~24 clés — et appelle 0x103C08C(obj, NOM, &local)); 0x103C08C = la PRIMITIVE fetch-regkey-u32 (120B: gardes, canary contre le global 0x418FFA0 — le 4.46 re-cadré, byte enable @0x41D3FDC, lookup 0x1A93218, `lw 0x10(row)` → *out, retour 0); 0x1A93218 = LE REGISTRE: liste chaînée RUNTIME (tête @0x40D1220, nœuds {next, valeur u32 @+0x10, état @+0x18, nom @+0x19}, comparaison insensible à la casse); LE STORE @0x130822C: la valeur → **config+0x3D84** (base c.lui 4+s3, disp -0x27c), le flag bit0 → **config+0x3D68** (bit1 = RMG5xL2VidmemPromote — le mot = la bitmap par clé), le push trace; LE CONSOMMATEUR @0x1318D4A (le scan -0x27c/-0x298 image-wide = 251 sites bruts → 5 après le filtre d'idiome c.lui-4+c.add): le byte gate config+0x67F, ways effectif = {non-posé→7, posé: 0→honoré (s2=0, config intact), 7→default (config intact), 1-6→CLAMPÉS à 7 (le store s2=7 prouvé)}, puis LA PROGRAMMATION REGISTRE: objet @*(s1+0x50), vtable (read+0x28/write+0x40), offset **0x2AC**, ways<<8 aux bits [15:8] avec les masques 0xFFFFE0FF/0xE0E10FFF | 0x10100000, puis 0x2BC — le knob atteint un registre MMIO (l'adresse absolue = INDECIDABLE-BY-BYTES, la base de fenêtre = état runtime). La carte §3 du runbook mise à jour: observe-only MAINTENANT avec l'EXPÉRIENCE FUTURE NOMMÉE (RML2MaxWaysSysmem=0 jugé par la batterie §1, son PROPRE jour). RmClkMprog FERMÉ: la fenêtre = la boucle notify/latch par entrée (méthode vtable @+0x68, latch @+0x38 testé==1 puis RESET sb zero, valeur @+0x34, compte @0x170(s1)) — le producteur de la valeur = la méthode de l'entrée (vtable runtime = INDECIDABLE, le dump du ring trace nommé désambiguïsateur). LE RE-SCOPE 0x418FFA0 = le global CANARY (load-xor-check) — la double citation 4.46 tenue honnêtement. Le census data famille = 1 hit (le NUL de RMDisableFbAddressRetraining — bruit): le pattern MCLK_LIMIT ×4 ne s'étend PAS à la famille L2 (consommation par code uniquement). LA CHASSE TIMING (v449b) — la moitié statique ÉPUISÉE avec preuve: (1) le scoring de features est MORT comme discriminant (40 sites aléatoires: max 113 > meilleur candidat 88 — PROUVÉ); (2) ZÉRO co-localisation des constantes LHR (210/175) avec un site 0x4C — cohérent avec des records bit-packés et/ou un parse hors-RM; (3) la signature memcpy (li a2,0x4c+call) = 42 sites/37 avec call — bancé, non-discriminant; (4) la région la plus dense (9×0x4C+mul76 @0x1220xxx) = un lookup de table statique 60×76B @0x1C57390 (scan par clé u32 @+0, clés hash/fuse-like, 60≠65, zéro pointeur) — NOMMÉ non-timing. VERDICT: le §5 runbook (le dump DMEM des empreintes) reste L'EXPÉRIENCE DÉCISIVE de la TÂCHE B. LEDGER: PROUVÉ (la chaîne complète, le registre liste chaînée, le store, le consommateur, la boucle latch, la table 60×76, les planchers de bruit, tous les comptes); HYPOTHÈSE (0x2AC = la surface de partitionnement L2 — la lecture parsimonieuse; la double citation canary); INDECIDABLE-BY-BYTES (l'adresse MMIO absolue; le producteur MclkProg; quel walker 0x4C si existence; l'offset TLS T); REFUSÉ: rien — le pack SAFE day-1 inchangé. Leçons d'instrument: l'octet indécodable DOIT avancer la marche (sz ou 2 — la première course v449a = OOM en boucle silencieuse sous le plafond 4 GB); capstone sans detail RAISE sur .operands; le scan de déplacements exige le filtre d'idiome de base (251→5); MESURER le plancher de bruit avant de croire un score; la plage famille = la run OBSERVÉE (la première plage large avalait les tables HUBCLIENT — 354 faux consommateurs)** | v449a_knob_store + v449b_stride_narrow (+JSONs); tools/edpp/runbook-447.sh §3 maj; findings-4.49-knob-store.md |
| 4.49 | knob-store | **LE STORE DU KNOB L2 NOMMÉ BOUT-EN-BOUT — la TÂCHE C répond, la chasse timing s'épuise statiquement. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact; la loi 512/512; les totaux stride 4.48 re-dérivés 349/185/15 exact). LA CHAÎNE RML2MaxWaysSysmem PROUVÉE: la fonction d'ingestion @0x1307AFC (s3=a1=la base config; la séquence par clé @0x1307B20-0x1307F1A matérialise chaque nom interné du bloc FB/L2 — GC6_CTX_HDR…RmEnableL2CohErrorIntr ~24 clés — et appelle 0x103C08C(obj, NOM, &local)); 0x103C08C = la PRIMITIVE fetch-regkey-u32 (120B: gardes, canary contre le global 0x418FFA0 — le 4.46 re-cadré, byte enable @0x41D3FDC, lookup 0x1A93218, `lw 0x10(row)` → *out, retour 0); 0x1A93218 = LE REGISTRE: liste chaînée RUNTIME (tête @0x40D1220, nœuds {next, valeur u32 @+0x10, état @+0x18, nom @+0x19}, comparaison insensible à la casse); LE STORE @0x130822C: la valeur → **config+0x3D84** (base c.lui 4+s3, disp -0x27c), le flag bit0 → **config+0x3D68** (bit1 = RMG5xL2VidmemPromote — le mot = la bitmap par clé), le push trace; LE CONSOMMATEUR @0x1318D4A (le scan -0x27c/-0x298 image-wide = 251 sites bruts → 5 après le filtre d'idiome c.lui-4+c.add): le byte gate config+0x67F, ways effectif = {non-posé→7, posé: 0→honoré (s2=0, config intact), 7→default (config intact), 1-6→CLAMPÉS à 7 (le store s2=7 prouvé)}, puis LA PROGRAMMATION REGISTRE: objet @*(s1+0x50), vtable (read+0x28/write+0x40), offset **0x2AC**, ways<<8 aux bits [15:8] avec les masques 0xFFFFE0FF/0xE0E10FFF | 0x10100000, puis 0x2BC — le knob atteint un registre MMIO (l'adresse absolue = INDECIDABLE-BY-BYTES, la base de fenêtre = état runtime). La carte §3 du runbook mise à jour: observe-only MAINTENANT avec l'EXPÉRIENCE FUTURE NOMMÉE (RML2MaxWaysSysmem=0 jugé par la batterie §1, son PROPRE jour). RmClkMprog FERMÉ: la fenêtre = la boucle notify/latch par entrée (méthode vtable @+0x68, latch @+0x38 testé==1 puis RESET sb zero, valeur @+0x34, compte @0x170(s1)) — le producteur de la valeur = la méthode de l'entrée (vtable runtime = INDECIDABLE, le dump du ring trace nommé désambiguïsateur). LE RE-SCOPE 0x418FFA0 = le global CANARY (load-xor-check) — la double citation 4.46 tenue honnêtement. Le census data famille = 1 hit (le NUL de RMDisableFbAddressRetraining — bruit): le pattern MCLK_LIMIT ×4 ne s'étend PAS à la famille L2 (consommation par code uniquement). LA CHASSE TIMING (v449b) — la moitié statique ÉPUISÉE avec preuve: (1) le scoring de features est MORT comme discriminant (40 sites aléatoires: max 113 > meilleur candidat 88 — PROUVÉ); (2) ZÉRO co-localisation des constantes LHR (210/175) avec un site 0x4C — cohérent avec des records bit-packés et/ou un parse hors-RM; (3) la signature memcpy (li a2,0x4c+call) = 42 sites/37 avec call — bancé, non-discriminant; (4) la région la plus dense (9×0x4C+mul76 @0x1220xxx) = un lookup de table statique 60×76B @0x1C57390 (scan par clé u32 @+0, clés hash/fuse-like, 60≠65, zéro pointeur) — NOMMÉ non-timing. VERDICT: le §5 runbook (le dump DMEM des empreintes) reste L'EXPÉRIENCE DÉCISIVE de la TÂCHE B. LEDGER: PROUVÉ (la chaîne complète, le registre liste chaînée, le store, le consommateur, la boucle latch, la table 60×76, les planchers de bruit, tous les comptes); HYPOTHÈSE (0x2AC = la surface de partitionnement L2 — la lecture parsimonieuse; la double citation canary); INDECIDABLE-BY-BYTES (l'adresse MMIO absolue; le producteur MclkProg; quel walker 0x4C si existence; l'offset TLS T); REFUSÉ: rien — le pack SAFE day-1 inchangé. Leçons d'instrument: l'octet indécodable DOIT avancer la marche (sz ou 2 — la première course v449a = OOM en boucle silencieuse sous le plafond 4 GB); capstone sans detail RAISE sur .operands; le scan de déplacements exige le filtre d'idiome de base (251→5); MESURER le plancher de bruit avant de croire un score; la plage famille = la run OBSERVÉE (la première plage large avalait les tables HUBCLIENT — 354 faux consommateurs)** | v449a_knob_store + v449b_stride_narrow (+JSONs); tools/edpp/runbook-447.sh §3 maj; findings-4.49-knob-store.md |
| 4.50 | 600gb-roadmap | **THE 600 GB/s ROADMAP + THE COMPLETE FB/L2 CARDS + THE INSTRUMENTS MACHINE. THE BANKED COUNTS REPRODUCED FIRST (auipc 416,206 exact aux deux alignements; la loi 512/512) ET TOUTES LES BATTERIES RESTENT VERTES APRÈS MODIFICATION (TT 11/11; TF 9/9; TR 18/18; selftest 5/5 — les nouvelles ajoutées, rien de cassé). (1) LA ROADMAP (ROADMAP-600GBs.md, en français pour le founder): les QUATRE sens de « 600 » décodés (M1 la métrique 0xSero ~409 effectif stock; M2 l'OC mclk ~440-485; M3 le 600+ L2-résident — garanti dès le Tier 1 pour les empreintes ≤ ~4 MB; M4 le mur streaming ~20.5-21.5 Gbps/pin = HARDWARE, documenté, HORS PÉRIMÈTRE décision PC-only 2026-09-21) + l'escalier Gbps→théorique→effectif + les TIER 0-4 à gates quadruples + le parking lot ingénieux (RMG5xL2VidmemPromote=1, le pack FB/L2, l'accounting rfc, l'overlap CE+SM, la sonde compression) + le calendrier J1-J5. (2) v450a — LA QUESTION BIT1 TRANCHÉE (PROUVÉ): le prologue v449a était FAUX (le test `(hw&3)==1 && (hw>>13)==3` attrape c.lui ET c.addi16sp — le discriminant = rd==x2); la VRAIE fonction du consommateur = c.addi16sp sp,-0x180 @0x1318CC0 → ret @0x131AAC8, 2296 insns; le census DANS la fonction entière: 1 charge du flag word, 2 accès ways, 12 bit tests — ZÉRO test du masque 2; le fallback image-wide (±0x40 des 251 sites bruts -0x298): UN candidat @0x12aa87a `andi s5,s5,2` — décodé = bit1 d'une LECTURE MMIO (vtable+0x28), le flag word n'étant que le multiplicande d'un calcul temporel ×1e9 — FAUX POSITIF CLASSÉ; VERDICT: bit1 (config+0x3D68) posé par l'ingestion mais LU PAR RIEN dans la classe déplacement. (3) v450b — LA CARTE D'INGESTION FB/L2 (24 clés nommées, fetches 0x1307b3e-0x1307f28) ET LA CORRECTION 4.49: le « voisin pose bit1 pour RMG5xL2VidmemPromote » = un ARTEFACT du dense-packing (la fenêtre 0x140 B avalait les voisins); le masque-2 ori appartient à RMFermiL2CacheBypass (@0x130829e: ori @0x13082a6 + sw du flag @0x13082aa — mask 2 = bit 1); G5x pose AUCUN bit: sa chaîne CORRIGÉE = u32 → config+0x85C4 ET config+0x85E4 (l'apply @0x1307d88-db6, base c.lui 8+s3) avec DÉFAUT SUR MISS = la constante 0x15511554 (le stub @0x130816a charge la magie et saute EN ARRIÈRE dans l'apply — le stubs sautent en arrière, les stores vivent dans les blocs d'application après chaque fetch); la carte byte-flag config+0x86xx nommée clé par clé (EccErrors→0x86B4, ZBC→0x8679, AsrWakeup→0x867D forcé à 1, AltL2ArbCYA→0x86A5 snez, PreFill→0x86B6 forcé à 1, ...); le chasseur des LECTEURS de 0x85C4/0x85E4 PARCÉ honnêtement (96 sites bruts, zéro idiome CFG8 au lookback ±26 B — INDECIDABLE-BY-BYTES). (4) LES INSTRUMENTS MACHINE (validés SANS GPU): --test-timings (TT-T 5/5 PASS: la table LHR byte-exact, le swap A/B launch↔LHR, le rollback, le slot 2→7, ret propre — la MACHINERIE du scénario timing validée, le layout byte-lane restant INDECIDABLE jusqu'au dump §5); build_timing_payload.py (8/8: les QUATRE représentations fingerprint des DEUX records reproduites avant toute émission; tables u64 paramétrées u8/u16/u32; le bloc C à TARGETS placeholder); pillarB.cu + pillarB-driver.sh (la méthode 0xSero: float4 grid-stride déroulé, les variantes streaming __ldcs/__stcs anti-thrash L2, le mode CE, l'overlap CE+SM, le mode persist = la lane M3 accessPolicyWindow avec le sweep 256 KB-4 MB et le garde cudaCtxResetPersistingL2Cache; verdicts JSON % de CEILING); runbook-447 §6 (l'intégration nvcc + la sonde THERMIQUE froid/chaud = le coût refresh GDDR6 + la sonde ncu dram__throughput %). LEDGER: PROUVÉ (les bornes vraies du consommateur, le verdict bit0-only, le faux positif classé, la carte 24 clés, la correction bit1=FermiL2, la chaîne G5x deux-mots + défaut 0x15511554, TT-T 5/5, 8/8 fingerprints); CORRIGÉ (la lecture 4.49 du bit1 G5x = artefact de fenêtre); HYPOTHÈSE (la sémantique des byte-flags 0x86xx = enables, le packing de 0x15511554); INDECIDABLE-BY-BYTES (le layout byte-lane des records parsés — le §5 décide; les lecteurs de 0x85C4/0x85E4; le % plafond stock — la batterie); REFUSÉ: rien tourné, rien flashé, le M4 hors périmètre. Leçons d'instrument: le discriminant prologue c.lui/c.addi16sp = rd==x2 (v449a a commencé une fonction de 7.5 KB en plein corps); les stubs dense-packés 4-12 B se tranchent par cibles de branches TRIÉES + base portée + coupe au premier saut inconditionnel; `ori x,x,2` = bit 1 (le masque, pas l'index) — un nom faux décale toute la carte; capstone imprime ici les cibles compressées en relatif — validé en retombant exactement sur 0x130822c** | v450a_g5x_bit1 + v450b_fb_l2_map (+JSONs); tools/booter_emu.py --test-timings; tools/edpp/build_timing_payload.py; tools/edpp/pillarB.cu + pillarB-driver.sh; tools/edpp/runbook-447.sh §6; ROADMAP-600GBs.md; findings-4.50-600gb-roadmap.md |

## The current state of the 280 W question (after 4.34)

- **4.34 verdict: the rm.elf lane is CLOSED — REVERT.** The seven
  250000 sites are TIME logic (proven 4.32); the 7/7-complete patch
  exists as the completeness proof (fwimage sha 5962342b…, v434a), NOT
  as a boot recommendation; `runbook-280.sh` refuses the half-turned
  6/7 container and gates the 7/7 behind RUNBOOK_77_ACK=1.
- The tick domain is resolved: rdtime = 1 tick = 1 ns (proven 4.34
  Part C); the policy thresholds stay µs (the 4.32 ladder); 100000 =
  the fine quantum (100 µs vs raw rdtime deltas).
- The power limit value **never travels** the fn=76 transport (the
  250000 = the LACT clock offset, the falsification).
- The enforcement = **the GSP-RM's EDPp policy object** (the 0x6d0
  object, the 4.20/4.21 passes), runtime-fed through a vtable (4.32
  TASK 3) — **the host-side limit API is the lever** (4.34).
- **The only road to the real data = pass 4.26: the recv-hook capture**
  (the response-path instrument = merged and armed; the boot = the
  capture, on the STOCK firmware).
- **Round 2 (4.35) adds the SAFE lane:** the host-regkey surface is
  proven real (864 names, 251 with code consumers, lookup-by-name +
  tables + FNV-1a-32) — the no-patch lever list and the knob cards
  live in `findings-4.35-optimization-hunt2.md`.
- **4.37 arms the two boot days:** the data twins are
  descriptor-linked (25 pointers, the fill path = the RPC
  object-create path the capture watches) — `runbook-426.sh` = day 1
  (the capture), the 4.37b regkey cards = day 2 (one key, one boot,
  one delta).

## The instruments (the naming convention)

`v4XX_<topic>.py` = the pass's instrument; `<topic>.json` = its output.
The chained verification = the flat results re-derived before every new
pass (the discipline since 4.19).
